///setPlayerAnimationIdle()
//This function calls a few functions to make the player assume the idle position. Simply a helper function to keep everything tidy.

setPlayerDirectionSpriteInt(0);
setPlayerImageSpeed("idle");
